# TODO — Deep Dominion

## Engine & simulation

- Wire the 4 non-proc monster abilities (`charge`, `smash`, `berserk`, `charm`) — they need bonus-damage / self-buff / morale hooks the engine lacks.
- Wire the `tile_transform` and `polymorph` spell effect types; `corrupt_land`, `make_earth` and `chickenify` currently fall through the dispatcher and do nothing.
- 5 hero abilities (dispel, purify, backstab, teleport, mass_cleanse) stay inert until ritual-detection, stealth and trap-state subsystems exist.
- Gem seams now pay their authored `resources.mine_value`, and a test pins the GDD property: a seam pays less per dig than a vein and is never consumed. The remaining question is **tuning**, not plumbing — 25 vs a vein's 100 is a 4x gap, which may make seams not worth an imp's time. Wants the playtest program.
- Creature social dynamics: same-faction hostility is hard-off, so there is no infighting, species rivalry or brawl-breaking.
- Room efficiency mechanics: adjacency bonuses, door placement, shape penalties.
- Rival keeper economy: digs and builds instantly and spawns free reinforcements; no traps, spells or research. Also support multiple simultaneous rivals.
- Trap ammunition/reload supply chain (imps rearm traps), magical door locking, alarm traps that summon defenders.
- ~~Reinforced wall construction (in the GDD, absent from tiles/data).~~ **That description was wrong** — `reinforced_wall` has been in `tiles.json` all along (`diggable: false`, `durability: 2000`) with art to match; what was missing was anything that *produced* it. The Stone Warden now does, via the `stonebinding` trait. What remains is the *player-directed* version: there is still no way to order a specific wall reinforced, only to station a warden near one.
- Environmental hazards as gameplay: lava/water damage and movement effects.
- Fog-of-war scouting gameplay; the tile field exists but no mechanics use it.
- Investigate the residual visibility leak that let a player see the rival keeper's lair.
- Hand interactions for gold and objects, not only creatures.
- Formation system and ranged combat improvements.
- A conversion-count trigger, so "convert N heroes" style `custom` objectives become winnable win conditions.
- Map generator: quality metrics with regenerate-on-poor-quality, and chunked generation for large maps.
- Scope decisions to take (build or cut): possession mode, overworld/surface raiding, meta-progression (faction runs, cross-mission unlocks), trade/hiring economy. **Creature mutations are now built** — `engine/mutation.rs` reads the `progression.mutations` that had been sitting in the data since before anything consumed it. What remains is presentation, not mechanism: a mutation currently announces itself with a notification and nothing else — no transformation effect, no entry in any creature list, and the player has no way to see *which* rooms a creature is close to evolving through.

## Content

- `docs/ROOM_SET.md`'s commit set is **built** — all 7 (Vault, Mana Well, Leisure Den, Arcane Archive, Combat Pit, Soul Furnace, Gatehouse) have art, a `rooms.json` entry, a gating tech and mission availability. Remaining from that doc: the Gatehouse ships as a defensive *bonus* room, not the "gates auto-close under threat" version — that wants a threat-detection subsystem it shares with alarm traps and the fog-of-war item. Decide whether to build it or restate the room as it now is.
- `tests/live_data_fields_tests.rs` sweeps **every** `pub struct` under `src/data/` for fields that are parsed and read nowhere, so a new struct cannot arrive with dead fields unnoticed. The first full sweep found **48**, now listed in `UNCONSUMED` with reasons. That list is the honest inventory of authored-but-inert content data; the biggest clusters are worth their own items:
  - **Room build time** — `construction_time` is still ignored, so rooms appear the instant they are paid for rather than being raised. Note before building this: the authored values are 0.4–0.9s, so wiring them as written buys a sub-second delay in exchange for a save-format change (per-tile progress) and renderer work to show a part-built room. Either re-scope the numbers to something a player would notice, or drop the field. The rest of the build rules (`allowed_terrain`, `requires_claimed`, `can_overlap`, `dig_required`, `max_instances`, `forbidden_if`) now go through `room_validator::tile_permits_room` / `dungeon_permits_room`.
  - **The rest of the hero behaviour model** — `trap_awareness`, `fear_resistance`, `will_fight_to_death` and `bravery` are wired. Still inert: `door_break_chance` (heroes never attack doors — they path around them, so there is nothing to roll against), ~~`light_preference`~~ (**wired** — see the lighting items under Visuals), `call_for_aid` (needs a hero rally mechanic; the natural shape is a threatened hero pulling nearby allies onto their attacker).
- Per-dig mining yields are now read from each tile's `resources.mine_value` in `tiles.json`, with the `imp_behavior.*_reward` config constants as fallback for tiles that declare none. All three resource tiles author their own, and a test requires that — otherwise a value silently reverts to the global constant, which is how the gem seam's 25 came to exist in two files at once.
- **A bug class the field guard cannot see** — fields that are *displayed* but not *enforced* look alive to any reference-counting check. Two found and fixed: `global_rooms_required` (the build button promised "Requires: Library" while the engine let you build without one) and `requirements.research` (a parallel tech list that drifted from the real gate in 7 of 18 rooms, and is now deleted in favour of deriving from `technologies.json`). The UI renders no other unverified promise — `LOCKED` reads `is_room_unlocked`, which is the real gate. The general lesson worth carrying: **any datum duplicated between two files will drift, and the copy the engine does not read is the one that lies.**
- Hero-building destruction effects are live and now **explain themselves** — razing the armoury reads "Armory destroyed — every hero loses 2 attack and 2 defence" rather than just naming the building. Remaining follow-ups:
  - The penalties **stack globally**: two razed barracks each contribute their full 50%, reaching the 90% cap. Whether that is right is a design question (it does reward levelling more of the base) rather than a bug.
  - Now observable: the `raid` capture scene seeds an army and reaches a **mission victory** around 6600 frames, which is the first end-to-end win this repo has recorded. Building the scene surfaced a real property worth knowing — the hero base sits behind solid rock and creatures do not dig, so the keeper cannot assault it unaided at all; the raiders have to follow the corridor the heroes tunnel in through. That is a genuine asymmetry (heroes come to you) and may be worth an explicit design decision rather than an accident of the map.
- Hero nerve is live *and now reachable*: `effective_retreat_threshold` folds `bravery` and `fear_resistance` into the authored `retreat_below_health`, `will_fight_to_death` removes it, and `should_reconsider_goal` lets a broken attacker break off. Two corrections to what I wrote when I shipped the threshold:
  - It is not just "the champion of light" — **9 of 20 heroes** are authored `will_fight_to_death`, including the `battle_cleric` that spawns in wave 1. Nearly half the roster never retreats. That is faithful to the data, but it is a much broader balance property than one late-game outlier, and it wants a real playtest.
  - The threshold had **no effect on attackers at all** until now: `should_reevaluate` returned false for `DestroyHeart`, so `decide_hero_goal` never ran for a hero on the assault. Traced a live wave to confirm — zero goal transitions in 2500 frames. Fixed narrowly (attackers press on unless actually broken) rather than by letting them re-plan freely mid-wave, which would have them wandering off to steal gold.
- The Soul Furnace burns *hero* corpses only, matching the graveyard. Dead creatures still vanish with nothing to show for them — decide whether your own fallen should be renderable, which is a tone question as much as a balance one.
- The temple is available in only 1 of 13 missions (`pacts_and_sacrifice`), so its `mana_generation_per_second` and the whole prayer loop are nearly unreachable in the campaign. Either widen its availability or accept that the ritual circle is the real mana room.
- `execute_sleep`, `execute_eat` and `execute_deposit_gold` still test `room_type ==` a specific room. These are *not* the same edit `research` and `train` were — each needs a data decision first, so don't generalize them mechanically:
  - **sleep** — the `sleep` family is `lair` *and* `kennel`, so widening it lets creatures sleep in kennels. Probably right (a kennel is where beasts sleep), but it interacts with `count_available_lair_tiles`, which sets the creature cap. Decide the cap question first. Until then the kennel's authored `sleep_recovery_rate: 1.2` stays unreachable — the lair's 1.0 is the only one the engine can see.
  - **eat** — the hatchery is `task_type: "work"`, so there is no `eat` family to match on. Needs a `task_type` reassignment, which changes what `execute_work` sees.
  - **deposit** — the treasury is `task_type: "none"`, shared with graveyard, vault, mana_well and leisure_den. Keying on it would make the graveyard a treasury. Needs its own task type.
- The rest of the amenity tier (Mentor's Den, Doctrine Chamber) is pure data now that `happiness_modifier` is live — but `docs/rooms.md`, which `docs/ROOM_SET.md` cites as their design source, **does not exist**. Either write the designs or drop the rooms; there is nothing to build from.
- Creature AI has no *need* that an amenity room satisfies, so creatures only reach the Leisure Den by wandering into it. A `comfort` need in `monsters.json` with `satisfied_by: ["leisure_den"]` would let them seek it out deliberately, the way they already seek food and sleep.
- Author the remaining `docs/monsters.md` roster. Eighteen are done — undead (**Lich**, **Grave Hulk**, on `necromancy`), demons (**Balor**, **Infernal Hound**), casters (**Cultist** on `ritual_tech`, **Hexbinder** on `war_magic`), tanks (**Ogre** on `blood_sport`, **Ironbound** on `fortification`), fighters (**Gnoll** on `beast_handling`, **Kobold** on `trap_tech`) the worker **Stone Warden** (`fortification`) the meta pair (**Overseer** on `logistics`, **Archivist** on `codification`) the **Beastling** on `beast_handling` the **Flesh Amalgam** on `necromancy` and the **Void-Touched** (mutation-only) — plus the **Hobgoblin**, which is not in docs/monsters.md at all but which the goblin has been trying to become since the beginning. Eighteen with the **Shadow Stalker** on `arcane_mastery`, which the lighting pass unblocked. **Only the Assassin Wisp remains**, and it is still blocked for the reason recorded below — its signature ability is already true of every creature.
- **Tile auras are live, and `bone_floor` finally does what it has always claimed.** It has declared `hero_fear: 0.1` over a radius of 2 since long before there was a fear mechanic to hang it on — the Ogre's `terrify` gave `fear` a meaning two iterations ago, and this connects the two. Standing near one raises a hero's breaking point, so a keeper can *build* ground that frightens. Three notes:
  - Read at the point of use rather than applied as a timed status, matching `needs::room_happiness_at`. That dodges the obvious trap: an aura applied every tick would stack without limit and a hero who lingered would become permanently terrified.
  - It scans outward from each **hero**, not over the map. Auras have small radii, so that is a couple of dozen tile lookups per hero per tick, where sweeping the map for aura tiles would be O(map) every frame.
  - New guard `every_tile_aura_effect_is_one_the_engine_acts_on`, because an unrecognised effect key is silently ignored — the same fail-quiet shape as mutation conditions and creature abilities.
- **`SpecialData::cannot_be_modified` is deleted rather than wired.** The only tile declaring it, `ancient_rune_floor`, already authors `diggable: false` and `claimable: false` — which the engine *does* read. A second flag saying the same thing is precisely the drift hazard this file keeps recording, so the right move was to remove it, as with `requirements.research`. `triggers_event` stays inert: there is no event system to fire `ancient_awakening`.
  - **The Void-Touched costs you something, which is the first time a creature has.** "Attracts attention" is wired: a `threat_contribution` trait field feeds `effective_threat_multiplier`, which sets *both* the gap between hero waves and how fast the hero garrison replenishes. Keeping one shortens your peace. Capped at doubling so a horde cannot drive the wave clock to zero, and it counts only living creatures — killing your own relieves the pressure it caused. Each property has a test, and all of them were verified by zeroing the contribution.
    - It is **mutation-only**, like the Hobgoblin: a Flesh Amalgam left beside a ritual circle corrupts into one. You do not summon a Void-Touched; you make one by accident. That chains the mutation system into the grafting system, and its sprite deliberately reuses the amalgam's lopsided frame drained to near-black with the seams glowing through — corruption should read as something happening *to* a creature rather than as a different creature.
    - `effective_threat_multiplier` now takes `&GameData`, since traits have to be resolved to sum the contribution. It is called twice per tick in `hero_spawner` and each call scans the creature list — small at current scale, but it belongs on the spatial-index item under Code quality if creature counts ever grow.
  - **The Flesh Amalgam is the only creature whose behaviour is not predictable from its name.** Traits were previously a property of a creature *kind*, so two goblins were interchangeable. `CreatureState::extra_traits` gives an individual its own, and the `grafted` trait rolls three of them once from a pool that `traits.json` opts into with `graftable`. The opt-in matters: rolling `stonebinding` or `commanding` onto a random amalgam would be absurd, so nothing is eligible unless it says so.
    - **This exposed a real split.** Trait modifiers were resolved in *two* places — `needs::active_traits` and a second, inline copy in `combat::extract_combat_stats` — so a grafted `strong` would have raised the creature's mood and left its attack untouched. Both now go through one `creature_traits(creature, monster_data, game_data)`. Verified by reverting the combat site and watching the test fail.
    - The pool is sorted before rolling because `game_data.traits` is a `HashMap`: unsorted, the graft would depend on hash iteration order as well as the RNG.
  - **`every_creature_can_actually_be_obtained`** — fifteen creatures were added to this roster in quick succession, each wired through a different route (tech unlock, mutation target, scenario availability). A creature with art, stats and no route is content nobody can ever see. Every creature now has a route; the imp is the one deliberate exception, spawned by the dungeon heart itself.
  - **The mutation system was authored, dangling and unread all at once — the quietest version of this repo's recurring failure.** The goblin has declared a `hobgoblin` mutation (level 5, twenty training-hall tiles) since before this TODO existed. `hobgoblin` was not in the roster, and *nothing read `mutations` at all*, so the broken reference and its missing consumer hid each other: no test could see a dangling target because no code looked at targets. Both now exist. Two guards were added at once — `every_mutation_target_exists` and `every_mutation_condition_key_is_understood` — and both were verified by planting typos.
    - The condition vocabulary was **derived from the one authored entry** rather than invented: `level_at_least`, and `<room_type>_tiles` for total owned tiles of a room type. That second key is exactly what "evolves based on room exposure" means, so the Beastling needed no new vocabulary — it branches into a hellhound beside a kennel or an **ogre** beside a combat pit, using creatures that already existed.
    - **Unknown condition keys fail closed.** A typo makes the mutation never fire rather than fire immediately, which is the safe direction: a mutation that never happens is invisible, but one that fires on turn one replaces a creature the player just paid for. A test pins this.
    - Mutating carries the *wound fraction* across rather than the absolute health, so evolving mid-fight is not a free heal.
    - Fixture note: `Room::new` leaves rooms `active: false`, and tile counts only include active rooms — correctly, since a half-built room should not push a creature over a threshold. Three tests failed until the fixture said so explicitly.
  - **The Overseer's aura is the first creature effect that acts on other creatures.** `commanding` multiplies the work efficiency of everything within `command_radius`; `scholarship` (the Archivist) multiplies the holder's own. Both land in `calculate_work_efficiency`, which is the single term `execute_work` *and* `execute_research` scale by, so one hook reaches production and research alike. Best commander wins rather than stacking, and an overseer does not command itself — each has a test.
    - The aura is cached on the creature (`command_bonus`, recomputed every tick by `command_aura::apply_command_auras`) rather than computed where used. That is forced by a borrow, not preference: `calculate_work_efficiency` runs while the working creature is already mutably borrowed out of `EntityManager`, so it cannot scan for neighbours at that point.
    - `calculate_work_efficiency` had taken `_monster_data` and ignored it since it was written. `scholarship` is the first thing to use it, so the underscore is gone.
  - **A balance test failed for a reason that was not a balance problem, and the metric was what needed fixing.** `test_creature_wage_efficiency_variance` compares `health / wage` across the roster and requires under 5x spread. Support creatures break its premise — nobody buys an Archivist for its hit points — and adding two of them pushed it to 5.4x. Tuning their wages to pass would have been fitting the data to the test; instead the check now skips creatures carrying a support trait, and says why. The check still means something for the fighters it was written about.
  - **The field guard was fooled by its own prose.** `live_data_fields_tests` counted a field as read if its name appeared anywhere in `src/` — *including comments*. Writing "the aura is for the workers" in an unrelated doc comment was enough to make `SpecialData::aura`, which is genuinely dead, look alive. Comments are now stripped before counting, which immediately surfaced two more real dead fields: `TileData::special` (parent of three already-listed fields) and `MutationData::conditions`. Both added to `UNCONSUMED` with reasons.
  - `task_system.rs` had reached **801 lines against the 800 hard limit**, so its inline `mod tests` moved to `src/task_system_tests.rs` — matching the convention ten other test modules already use. They only ever exercised the public `execute_task`, so nothing had to be widened. The file is now 493 lines.
  - **The Stone Warden reinforces walls for real, and this is the first creature that changes the map.** `stonebinding` converts an adjacent plain wall to `reinforced_wall` every 20s. It is not cosmetic: `diggable` is enforced in `hero_digging` *and* in `build_hero_pathfinding_grid`, so a reinforced wall genuinely closes a tunnel route — which matters because the `raid` scene showed heroes tunnel in and creatures do not. A test pins that property directly (`reinforced_wall` must stay undiggable) so the mechanic cannot decay into decoration by a `tiles.json` edit.
    - What counts as reinforceable is expressed **in tile data, not by naming tile types**: blocks movement, is diggable, has no `resources`, has no trap. That last-but-one clause is what stops a warden sealing away a gold vein the imps still want, and a test covers it.
    - It runs as its own per-tick pass rather than inside `creature_ai`, for two reasons worth remembering: `update_creatures` takes the dungeon *immutably*, and `creature_ai.rs` is at 781 lines against the 800 hard limit, so it had no room to grow.
    - Still missing: a **player-directed** reinforce order. You can station a warden, but you cannot point at a wall and say "that one".
  - **`trap_savvy` is the first trait that acts on the world rather than on its holder.** A Kobold within `trap_tending_radius` multiplies the damage of any trap sprung there, via two new `TraitData` fields. The best tender wins rather than bonuses stacking, so a crowd of kobolds in one corridor is not a multiplier to farm. It is an entity scan, but it runs when a trap *fires* — an event, not a frame — so it does not touch the per-frame O(n) scan problem under Code quality.
  - **The stealth tier was considered and rejected for now**, and the reason is worth recording: the Shadow Stalker's "stronger in darkness" needs a lighting system that does not exist, and the Assassin Wisp's "bypasses doors and traps" is *already true of every creature* — `process_trap_triggers` only ever iterates `entities.heroes()`, so no creature can trigger a trap. Authoring it would have shipped a signature ability that is a no-op for a reason unrelated to the creature. Door bypass would need a per-creature pathfinding flag.
  - Test note: no hero in the roster has `trap_awareness` of 0.0 (the lowest is the peasant's 0.2), so a single trap-trigger attempt is flaky one time in five. The trap tests retry with fresh state until it fires and assert the awareness is non-zero, so the day someone authors a 0.0 hero the retry loop can be deleted deliberately rather than found by a mystery failure.
  - Sprite note: a tail drawn as a cone points *up* the body's flank in this projection and reads as a stray limb — there is no clean way to lay one horizontally. The Gnoll dropped its tail and leans on the hyena shoulder hump, snout and ears instead. The Kobold is deliberately the smallest silhouette in the roster, filling about half the frame, since scale is the only reliable way to say "minor" when everything is drawn at the same 64px.
  - **`fear` is now a real status.** The Ogre's "causes fear waves" is wired: a `terrify` proc pushes a `fear` status, and `hero_ai::current_retreat_threshold` raises the health at which that hero breaks off, blunted by their own `fear_resistance`. It deliberately does **nothing** to a `will_fight_to_death` hero — that flag is documented as "dies where they stand" and nine of twenty heroes carry it, so letting fear overrule it would quietly contradict the authored data. This is the first thing that makes the hero nerve model matter to the *keeper's* choices rather than only to hero authoring.
  - **The Ironbound's "immune to morale" is a new `construct` trait**, not a special case: zeroed `need_decay_multipliers` for food/sleep/gold, `desertion_threshold_modifier: -100`, `discipline_response_multiplier: 0.0`. All of that is machinery `traits.json` already had. Its needs are authored at ordinary rates precisely so the trait is what makes them irrelevant — and a control test checks an orc in the same fixture *does* get hungry, so the immunity cannot be an artefact of the setup.
  - Its other half — "drains mana instead" — **has no hook**: `economy.wage_per_minute` is gold-only and there is no mana upkeep anywhere. Same gap as the Balor's "burns mana". Two creatures now want the same missing feature, which is the argument for building it.
  - New guard: `every_creature_trait_exists_in_traits_json`. Traits are tag strings and an unknown tag is not an error anywhere — the creature just gets no modifier — so a typo turns a design intent into silence. It was the one content vocabulary with no reference check.
  - Sprite note: sinking the Ogre's head between its shoulders (the obvious way to draw "brute") simply deletes the head, and what remains reads as a headless lump with ears. A heavy brow ridge with the head *clearing* the shoulders does the job. The Ironbound is the only creature drawn entirely from `draw_box_3d` — no curved surface anywhere — which is what makes a construct read instantly at 64px.
  - The **Hexbinder is the first creature whose identity is a mechanic rather than a stat line**. "Weak damage, strong control" is real: two new `ability_effects` entries — `hex` (a `freeze` at strength 0.55, so a 45% slow for 4s) and `curse` (a slow `poison` DoT) — go through machinery the engine already honours, and `freeze` scales `movement_speed` on application and divides it back on expiry for heroes as well as creatures. Its damage range is deliberately below the warlock's.
  - The Cultist is honest but not novel: a cheap ritual caster with `fireball`. **"Generates power through sacrifice synergy" has no hook** — `generate_room_mana` is `tiles × efficiency × rate` and does not know which creatures are standing in the room, so no creature can contribute to a ritual circle's output. Building that means giving room output a per-occupant term, which is the same shape as the "room efficiency mechanics" item.
  - `tests/content_wiring_tests.rs` now has `every_creature_ability_is_wired_or_declared_inert`: every ability on every creature must either have an `ability_effects` entry whose `status_type` some engine code acts on, or be named in an explicit `INERT_ABILITIES` list (the four already tracked at the top of this file). Both failure branches were verified by planting probes. This is the guard that would have caught authoring `smash` on the Balor.
  - **A trap it also closes:** `apply_combat_result` multiplies `movement_speed` only for `freeze`, but `expired_speed_multipliers` divides back out for `freeze` *and* `speed_modifier`. A creature ability authored as `speed_modifier` would therefore revert a multiplier that was never applied and leave its victim **permanently faster**. Spells push that status themselves and do apply the multiplier, which is why the asymmetry has never bitten. The new test rejects `speed_modifier` on a creature ability outright; the cleaner fix is to make application symmetric.
  - The Infernal Hound joins `demonology`; the Balor is gated a step deeper on `soul_rendering`, which already required `demonology`, so "endgame demon" is expressed as a real research chain (`ritual_tech` → `demonology` → `soul_rendering`) rather than only as big numbers. Its `max_population` is 1.
  - `docs/monsters.md` gives the Balor "burns mana, breaks walls, ignores fear". Only *ignores fear* is real: it is authored `fearless`. **Breaks walls has no mechanic** — no creature can damage terrain — and a literal mana upkeep has no `needs` key (the engine knows only sleep/food/gold/training). Deliberately not authored as `smash`, which is one of the four inert abilities already listed at the top of this file; adding it would have looked wired and done nothing.
- **`visual.sprite` was dead in both rosters, and had drifted in all 39 entries.** The texture loader derives `assets/sprites/{monsters,heroes}/{id}.png` from the roster key, so the authored path was never read — and every monster entry pointed at `creatures/…` while every hero pointed at `heroes/…`, two directories that have never existed. Field and JSON both deleted rather than authoring two more copies for the new demons. Two things this teaches:
  - **It is the exact bug class this file already documents, in the one place the guard cannot see it.** `tests/live_data_fields_tests.rs` counts a field as live if its *name* appears anywhere in `src/`, and `sprite` does — `TileVisualData::sprite` and `RoomVisualData::floor_sprite` are read constantly. A name shared with a live field is a blind spot by construction. Making the sweep struct-aware needs real parsing rather than text matching, so the practical mitigation is to distrust the guard specifically for common field names (`sprite`, `name`, `id`, `cost`, `icon`).
  - The asset-manifest test passed throughout, because it also derives paths from ids. **Both the engine and its test agreed with each other and ignored the data** — which is why nothing ever pointed at the discrepancy.
- `Material::fire()` is pure ambient (`ambient 1.0`, `diffuse 0.0`), so anything drawn with it comes out a flat untextured silhouette. That is fine for a 4px flame blob and useless for anything with a shape — the Balor's sword was an orange rectangle until it moved to a material with a diffuse response. Worth knowing before reaching for it.
- No scenario lists **any** undead in `availability.creatures` — not skeleton, zombie, ghost or vampire, and now not Lich or Grave Hulk either. That is consistent rather than an omission: the tier is reached through `necromancy` research, and `availability` only seeds the starting set. Worth confirming that is the intent before someone "fixes" it.
- **The sprite renderer's key light was inverted, and every sprite in the repo was drawn against it.** `shade_color` negated the diffuse dot product, so a surface with `nz = 1` — pointing straight *up*, which in this projection is most of any large rounded body — received zero diffuse and rendered at bare ambient. Small spheres survived because their lit right/bottom rim is a big fraction of their pixels; the knight, zombie, troll, bile demon and Grave Hulk were near-black blobs. Fixed, along with a second-order problem the fix exposed: the light also sat only ~24° off the view axis, which lights every visible normal about equally and renders bodies flat, so the key moved to ~38° off-axis (`-0.55, 0.10, 0.83`) and there is now a terminator across each subject. Three things worth carrying:
  - **The failure mode read as a style choice.** "Dark, murky dungeon creatures" is a plausible art direction, so nothing about the output said *bug* — it took deriving the shading maths by hand to see that upward-facing meant unlit. Generated output that looks deliberate is not evidence that it is.
  - The Blinn-Phong half-vector is precomputed, which makes it a **second copy of the light direction** — the same drift hazard as the data duplication elsewhere in this file. `half_vector_matches_the_light_it_was_derived_from` recomputes it from `LIGHT_*` and `TILT` and fails if someone moves the key light without updating it.
  - Every existing sprite was authored while looking at wrong shading, so some base colours may have been chosen to compensate for the darkness. A sweep of monsters, heroes and tiles after the fix showed no blowouts and tiles unchanged (they are flat 2D patterns that never went through the 3D primitives), but a colour pass is worth doing when the palette is next revisited.
- **Every generated tile is now reachable from data.** The casino was the last holdout and is deleted — generator, registration and PNG. The supersession was confirmed rather than assumed: `docs/ROOM_SET.md` line 51 names the Leisure Den the "Casino successor", that room shipped, and the casino appeared in no `rooms.json`, no `tiles.json` and nowhere in `src/`. `AWAITING_DATA` in `tests/asset_manifest_tests.rs` is now empty, which is the state it should stay in.
- Rooms declare a `visual.wall_sprite` (`tiles/lair_wall.png`, …) that no generator emits and nothing reads. Deciding this needs a renderer answer, not a data one: walls are `solid_rock`/`earth` tiles, so drawing a room's own wall art means a per-wall-tile adjacency lookup every frame — which collides with the O(n) scan problem already flagged under Code quality. Treat it as part of the lighting/atmosphere pass, or drop the field.
- Spell targeting is enforced: `valid_targets` refuses a creature of the wrong allegiance (you could heal an invading knight), `requires_visibility` refuses a fogged target, and every refusal now reaches the player as a notification instead of an `eprintln!`. Two deliberate limits — `valid_targets` is enforced only for `creature`-targeted spells, since refusing to place a fireball on an empty tile would stop you catching someone standing beside it; and the fog check mirrors the renderer's condition, so it does nothing while fog is disabled.
- Spell depth that needs engine hooks: miscasts, hero counter-spells, research-unlocked spell modifiers.
- Decide the fate of the mod/content-pack system: `mods/load_order.json` ships empty — market it (docs, examples, validation) or cut it.
- Wire campaign missions to grant *technologies* rather than raw unlocks, so research gates progression.

## Audio

- Audio engine layer with WASM support, built into `macroquad-toolkit` so every game benefits.
- SFX set (~60–100 sounds): digging, claiming, room build, gold pickup/deposit, combat, spells, traps, creature moods, UI, alerts.
- Music: main theme, ambient dungeon layers, raid/combat transitions, victory and defeat cues.
- Mixing, ducking, camera-relative panning, master/music/SFX volume settings with persistence.
- Sourcing decision: commission, license or produce — budget and pipeline.

## Visuals & game feel

- Animation: every sprite is a single static 64×64 frame. Pick a tier (walk/attack/death frames, or procedural bob/squash/flip) and budget it.
- Particles and feedback: hit flashes, damage numbers, death poofs, spell VFX, trap triggers, dig debris, gold sparkle, heart-damage screen shake — none exist.
- **In-game lighting exists now, and it renders the palette the designers already wrote.** `rooms.json` authored a `visual.light` — colour, intensity, flicker — for **22 of 24 rooms**, and `tiles.json` did the same for the mana crystal, lava and the ancient rune floor. Every byte of it was inert. `engine/lighting.rs` builds a light map and `renderer/tiles.rs` tints each tile by it, so the dungeon heart now sits in a pool of its own red and the ground falls off into dark. What remains of the atmosphere pass:
  - **`flicker` is still unread** — it needs a time-varying term, which means the light map stops being a pure function of the dungeon. Cheap to add once there is a frame clock to hand; deliberately not faked with a per-frame random, which would strobe rather than flicker.
  - The map is rebuilt **every frame**. It splats outward from sources (room tiles plus three tile types) rather than asking each tile what lights it, so it is a few thousand operations rather than every-visible-tile-times-its-neighbourhood — but it is still redundant work between dungeon changes, and belongs with the caching item under Code quality.
  - Ambient is 0.45: unlit ground is dim but never black. Fog of war is the system that *hides*; lighting only sets mood, and a pitch-black dungeon is unplayable.
  - The light map now lives on `GameState` and is rebuilt once per tick, not once per frame in the renderer. That was forced rather than chosen: the Shadow Stalker's combat reads it too, and two systems building the same map separately would have been the drift hazard this file keeps recording.
- **`light_preference` is live, and it makes darkness a two-edged decision.** All twenty heroes author it — twelve `bright`, seven `any`, and the rogue alone `dark` — and nothing had read it. It folds into the *same ambient-fear term* the bone floor's aura feeds, rather than into pathing. That choice is the point: one dark corridor now strengthens a Shadow Stalker **and** unsettles most of the hero roster, instead of being two unrelated systems that happen to consult the same light map. An end-to-end test shows a wounded knight breaking off in the dark and pressing on under a lamp at exactly the same wound.
  - It is a free-text string, so a typo would silently mean "indifferent". `every_hero_light_preference_is_one_the_engine_understands` closes that, verified by planting `candlelit` on a hero and watching it fail.
- **The Shadow Stalker makes the keeper's own lighting a tactical decision.** `lightless` grants `darkness_attack_bonus`, scaled by how dark the creature's tile actually is — its authored attack of 12 is modest on purpose, and nearly doubles in an unlit corridor. Building a lit room beside a patrol route now *weakens* the thing patrolling it, which is the first time the two systems argue with each other in a way the player can exploit.
  - Darkness is cached per creature (`CreatureState::darkness`) for the same reason `command_bonus` is: `combat::extract_combat_stats` is handed a creature and the game data, never the dungeon, so anything positional must be resolved beforehand.
  - A control test checks an orc fights identically lit or unlit, so the effect is the trait rather than the lighting model leaking into everyone's combat stats. Verified by zeroing the bonus and watching it fail at 12 vs 12.
- UI art overhaul — playtest feedback calls the UI dated and Rust-default. Includes real entity rendering, sidebar animation and the minimap viewport.
- Title/menu art beyond the single `main_menu_bg.png`. (The window icon ships — `graphics_gen/icon.rs` draws the dungeon heart at 16/32/64 and `main.rs` embeds it.)

## UI & UX

- Settings menu breadth: resolution and window modes, volume sliders, keybind remapping (keys are hardcoded in `engine/input.rs`), camera/scroll options, autosave toggle.
- Save system: every call site hardcodes `"slot_1"`. Needs a multi-slot UI, autosave, quicksave/quickload, save metadata, and format versioning/migration.
- Tutorial: 6 steps cover only dig→rooms→recruit. Extend to combat, traps, spells, research, prison/torture, temple, wages/moods/slapping, hero waves and the minimap, plus contextual hints and an intro.
- Hotkey overlay / help screen.
- Localization: no i18n layer, all strings hardcoded English — externalize strings before it gets expensive.
- Accessibility: colorblind-safe faction palettes, text scaling beyond 3 steps, hold-vs-toggle options, screen-shake toggle.
- Decide on gamepad support (probably cut, but Steam Deck verification wants basic navigation).

## Balance

- Hero/creature level asymmetry: heroes gain +15%/level to cap 10, creatures +10% to cap 5 — the late game mathematically favours heroes.
- Wave pacing: players are killed before they can set up, while the balance numbers claim a ~33-minute first wave. Reconcile config against the shipped build and scale pacing by difficulty.
- Starting gold scarcity near the dungeon core — the loudest playtest complaint.
- Room sell refund is 5%; 25–50% would make experimentation viable.
- Creature value outliers: Bile Demon overpriced, Hellhound free, Succubus mood loop, treasury desirability clustering.
- Creature wage and need decay rates still untuned.
- Structured playtest program: wave-1 survivability, sustainable army size, wave-10+ viability, per-mission tuning. The `wave` and `raid` capture scenes make the first two observable without a ten-minute sit — a first run showed wave 1 (2 heroes) reaching the dungeon heart and taking it to 988/1000 within ~15s of launching. Treat that as a starting point rather than a verdict: the scene seeds dig orders, which opens paths a real wave-1 dungeon would not have.
- Fold the `balance_calculator` simulations into `cargo test`/CI — its assertions are hand-rolled bools with no `#[test]`s.

## Code quality

- `PlayerState::warn_once(key, message)` is the pattern for any condition checked every tick that the player still needs telling about once — used by the treasury overflow and all four spawn-blocked reasons. Reach for it rather than a bespoke bool.
- Strip debug output. The swallowed-player-feedback subset is done: spell-cast failures, room build refusals (unresearched / not enough gold / not enough mana), trap refusals (unresearched / no Workshop / no crates) and treasury overflow all reach the player now. Save/load failures turned out to already notify — the `eprintln!` beside them is a genuine diagnostic. The spawner's four blocked reasons are done too. The per-tick tracing — 31 calls across combat, traps, imps, tasks, creatures, spells and hero waves — is now behind `trace_log!(tag, ..)`, silent unless `DUNGEON_MANAGER_LOG` names the tag. What remains unconditional is one-shot startup diagnostics (asset/font/data loading, map load, save/load) and four genuine warnings, which is roughly what should stay.
- The screenshot harness has `simulation` (intro dismissed, dig orders seeded — the game actually running) and `wave` (the same with the first hero wave pulled forward from 600s to 2s, so combat is reachable at all). `gameplay` still captures the briefing overlay, which freezes the simulation by design. Note the shared toolkit capture script does not forward extra env vars, so `DUNGEON_MANAGER_LOG` tracing needs the exe driven directly rather than through `scripts/capture_ui.ps1`.
- Error-handling hardening: ~25 `unwrap()`, 9 `expect()`, 4 `panic!` — asset loading and save handling especially.
- Deduplicate movement and distance logic between `creature_ai` and `imp_ai` (two `manhattan_distance` impls).
- Test coverage: combat math has one test, save/load one, UI/actions none. Add tests for menu action handlers, sidebar selection, tooltip state and dungeon command dispatch.
- Scenario fixtures for room placement, resource flow, encounters and progression milestones.
- Separate model mutation from the renderer and sidebar modules so UI actions call explicit domain commands.
- Performance on large maps: O(n) entity-position scans need a spatial index; cache pathfinding and room detection; profile the sidebar and renderer paths that recompute layout every frame.
- Data-driven stragglers: hardcoded imp claim delay, loose spawn-placement validation.
- Decide on the GDD's determinism/replay aspiration while it is still cheap.

## Release

- Versioning and builds: still v0.1.0; pin or vendor `macroquad-toolkit` for reproducible releases; add a native `[profile.release]` (the workspace default is tuned for WASM size).
- Storefront: no Steam or itch integration. Needs steamworks bindings (achievements, cloud saves, rich presence), depot/build scripts, store page, capsule art, trailer, screenshots.
- Demo build for Steam Next Fest — needs 2–3 polished missions.
- Windows installer/zip, a tested Linux build, opt-in crash reporting, and a patch strategy compatible with save migration.
- Legal: trademark check on "Deep Dominion", dependency license audit, asset provenance, EULA/privacy if telemetry ships.
- Marketing runway: press kit, devlog cadence, wishlist campaign.
- Fix `docs/gdd.md`'s stale "Bevy ECS" claim.
